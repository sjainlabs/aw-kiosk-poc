/**
 * Shared Angular name constants for the fast-compile path.
 *
 * Centralised here to prevent drift when decorator sets are referenced
 * across multiple source files (compile, registry, JIT, metadata).
 */
/** All six Angular class decorators. */
export declare const ANGULAR_DECORATORS: Set<string>;
/**
 * Matches a call to any Angular class decorator (e.g. `Component(` or `Service(`).
 * Derived from {@link ANGULAR_DECORATORS} so the fast-path "is this an Angular file?"
 * checks stay in sync with the canonical decorator set and don't drift.
 */
export declare const ANGULAR_DECORATOR_CALL_RE: RegExp;
/**
 * Angular decorators that produce compilable declarations (selector, template,
 * pipe name, or module exports).  Excludes `@Injectable` and `@Service`, which
 * self-register via ɵprov and don't need Ivy compilation.
 */
export declare const COMPILABLE_DECORATORS: Set<string>;
/** Decorator names used on class fields and host bindings. */
export declare const FIELD_DECORATORS: Set<string>;
/** Signal-based reactive APIs that need downleveling or metadata extraction. */
export declare const SIGNAL_APIS: Set<string>;
