/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.dev/license
 */
import { JavaScriptTransformer, } from './utils/devkit.js';
export function createCompilerPlugin(pluginOptions, isTest, closeTransformer, cache) {
    const javascriptTransformer = new JavaScriptTransformer({ ...pluginOptions, jit: true }, 1, cache);
    return {
        name: 'analogjs-angular-esbuild-deps-optimizer-plugin',
        async setup(build) {
            if (!isTest) {
                build.onLoad({ filter: /\.[cm]?js$/ }, async (args) => {
                    const contents = await javascriptTransformer.transformFile(args.path);
                    return {
                        contents,
                        loader: 'js',
                    };
                });
            }
            if (closeTransformer) {
                build.onEnd(() => javascriptTransformer.close());
            }
        },
    };
}
export function createRolldownCompilerPlugin(pluginOptions, isTest, closeTransformer, cache) {
    const javascriptTransformer = new JavaScriptTransformer({ ...pluginOptions, jit: true }, 1, cache);
    const plugin = {
        name: 'analogjs-rolldown-deps-optimizer-plugin',
    };
    if (!isTest) {
        plugin.load = {
            filter: {
                id: /\.[cm]?js$/,
            },
            async handler(id) {
                const contents = await javascriptTransformer.transformFile(id);
                return {
                    code: Buffer.from(contents).toString('utf-8'),
                    loader: 'js',
                };
            },
        };
    }
    if (closeTransformer) {
        plugin.buildEnd = () => javascriptTransformer.close();
    }
    return plugin;
}
//# sourceMappingURL=compiler-plugin.js.map