import type { Plugin } from 'vite';
export declare function buildOptimizerPlugin({ jit, }: {
    supportedBrowsers: string[];
    jit: boolean;
}): Plugin;
export declare function extractInlineSourceMap(code: string, id: string): {
    code: string;
    map: null;
} | {
    code: string;
    map: string;
};
